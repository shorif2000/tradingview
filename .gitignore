# derived caches — regenerate with data_io.load_ohlc(), never committed because
# pickle format is tied to the pandas version that wrote it
*.pkl
__pycache__/
*.pyc
backtest_report.html
trades*.csv

# build artefacts from the backtester
reports/
.cache/

# local tooling and the bridge's delete-workaround folder
.claude/
_to_delete/

# desktop app delivery folder, not source
Claude outputs/

# Duplicate browser downloads: "name-1.pine", "name (1).pine"
pine/*-[0-9].pine
pine/*(*).pine

# Local git bundles (history backups) — never commit these
.bundles/

# macOS Finder metadata
.DS_Store
